import React from 'react';
import { Layout } from 'antd';

const { Content } = Layout;

const AppContent = () => {
  return (
    <Content className="site-layout">
      <div className="site-layout-content">site-layout-content</div>
    </Content>
  );
};

export default AppContent;
